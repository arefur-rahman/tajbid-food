function SectionHeader({ header }: { header: string }) {
    return (
        <h1 className="text-sidebar-foreground text-3xl font-serif font-medium">
            Categories
        </h1>
    );
}

export default SectionHeader;
